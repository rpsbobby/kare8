apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kare8-service.fullname" . }}-test-connection"
  labels:
    {{- include "kare8-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kare8-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
