apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: consumer-monitor
spec:
  selector:
    matchLabels:
      app: kafka-consumer
  endpoints:
    - port: metrics
      interval: 5s
